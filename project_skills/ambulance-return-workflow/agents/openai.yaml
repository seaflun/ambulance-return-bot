interface:
  display_name: "Ambulance Return Workflow"
  short_description: "Maintain the ambulance return NAS task center, public-duty worker, package build, and release workflow."
  default_prompt: "Use $ambulance-return-workflow to update ambulance_return_bot and verify the public-duty plus generated NAS package workflow."
